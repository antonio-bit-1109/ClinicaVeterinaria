@model IEnumerable<ClinicaVeterinaria.Models.Cassetti>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descrizione)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdArmadiettoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descrizione)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdArmadiettoNavigation.IdArmadietto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCassetto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCassetto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCassetto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
